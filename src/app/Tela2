import React from 'react';
import {
  View,
  Text,
  StyleSheet,
  SafeAreaView,
  TouchableOpacity,
} from 'react-native';

export default function OnboardingScreen() {

  const handleNext = () => {
    console.log('Ir para a Home');
  };

  return (
    <SafeAreaView style={styles.container}>

      {/* Topo */}
      <View style={styles.topArea}>

        <View style={styles.steps}>
          <View style={styles.activeStep} />
          <View style={styles.step} />
          <View style={styles.step} />
        </View>

        <Text style={styles.skip}>Pular</Text>

      </View>

      {/* Ilustração */}
      <View style={styles.illustrationArea}>

        <View style={styles.sun}>
          <Text style={styles.sunText}>☀</Text>
        </View>

        <View style={styles.studentCircle}>
          <Text style={styles.studentEmoji}>🧑🏽‍💻</Text>
        </View>

        <View style={styles.bookArea}>
          <Text style={styles.book}>📖</Text>
        </View>

        <View style={styles.cactusArea}>
          <Text style={styles.cactus}>🌵</Text>
        </View>

      </View>

      {/* Conteúdo */}
      <View style={styles.content}>

        <Text style={styles.title}>
          Estudar fica mais fácil com um plano
        </Text>

        <Text style={styles.description}>
          Organize tarefas, provas e horários de um jeito simples,
          visual e feito para sua rotina.
        </Text>

      </View>

      {/* Botão */}
      <TouchableOpacity
        style={styles.button}
        onPress={handleNext}
      >
        <Text style={styles.buttonText}>
          Próximo
        </Text>

        <Text style={styles.arrow}>
          →
        </Text>
      </TouchableOpacity>

    </SafeAreaView>
  );
}


const styles = StyleSheet.create({

  container: {
    flex: 1,
    backgroundColor: '#FFF9F1',
    paddingHorizontal: 24,
    paddingTop: 10,
    paddingBottom: 25,
  },

  // TOPO

  topArea: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginTop: 15,
  },

  steps: {
    flexDirection: 'row',
    alignItems: 'center',
  },

  activeStep: {
    width: 28,
    height: 6,
    borderRadius: 10,
    backgroundColor: '#162A54',
    marginRight: 7,
  },

  step: {
    width: 28,
    height: 6,
    borderRadius: 10,
    backgroundColor: '#E3DED6',
    marginRight: 7,
  },

  skip: {
    fontSize: 14,
    color: '#162A54',
    fontWeight: '500',
  },

  // ILUSTRAÇÃO

  illustrationArea: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    position: 'relative',
  },

  studentCircle: {
    width: 190,
    height: 190,
    borderRadius: 95,
    backgroundColor: '#FFE3B3',
    justifyContent: 'center',
    alignItems: 'center',
  },

  studentEmoji: {
    fontSize: 95,
  },

  sun: {
    position: 'absolute',
    top: 80,
    right: 35,
  },

  sunText: {
    fontSize: 42,
  },

  bookArea: {
    position: 'absolute',
    bottom: 55,
  },

  book: {
    fontSize: 70,
  },

  cactusArea: {
    position: 'absolute',
    left: 28,
    bottom: 70,
  },

  cactus: {
    fontSize: 48,
  },

  // TEXTO

  content: {
    marginBottom: 30,
  },

  title: {
    fontSize: 30,
    lineHeight: 38,
    fontWeight: 'bold',
    color: '#162A54',
    marginBottom: 14,
  },

  description: {
    fontSize: 16,
    lineHeight: 25,
    color: '#4A4A4A',
  },

  // BOTÃO

  button: {
    backgroundColor: '#162A54',
    height: 58,
    borderRadius: 18,
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    position: 'relative',
  },

  buttonText: {
    color: '#FFFFFF',
    fontSize: 16,
    fontWeight: '600',
  },

  arrow: {
    position: 'absolute',
    right: 22,
    color: '#FFFFFF',
    fontSize: 24,
  },

});